import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.arbvisualizer.app',
  appName: 'Arb Visualizer',
  webDir: 'dist/public'
};

export default config;
